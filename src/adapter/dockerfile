FROM alpine:3.20

RUN apk add --no-cache python3 py3-pip
RUN pip install --break-system-packages requests

RUN addgroup -S stos
RUN adduser -S stos -G stos

WORKDIR /app
COPY adapter.py .

ENTRYPOINT ["python3", "adapter.py"]

#docker build -t master ./src/adapter
